% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_first_export.R
\name{get_first_export_cran}
\alias{get_first_export_cran}
\title{Find the first CRAN release exporting a symbol}
\usage{
get_first_export_cran(
  package,
  function_name,
  date_only = FALSE,
  repos = "https://cloud.r-project.org",
  cache_dir = getOption("ggext.cran_cache", file.path(tempdir(), "cran_export_cache"))
)
}
\arguments{
\item{package}{Character. The name of the CRAN package (e.g. \code{"ggplot2"}).}

\item{function_name}{Character. The symbol to search for (e.g. \code{"geom_point"}).}

\item{date_only}{Logical. If \code{TRUE}, return only the publication \code{Date}.
Defaults to \code{FALSE}.}

\item{repos}{Character. The CRAN mirror URL. Defaults to
\code{"https://cloud.r-project.org"}.}

\item{cache_dir}{Character. Directory used to cache parsed CRAN metadata.
Defaults to \code{getOption("ggext.cran_cache")}, falling back to a temporary
directory.}
}
\value{
If \code{date_only = TRUE}, a \code{Date} object or \code{NA}.

Otherwise, a one-row \code{data.frame} with columns:
\itemize{
\item \code{package}: package name
\item \code{version}: CRAN version where the symbol is first exported
\item \code{date}: CRAN publication date (may be \code{NA} for older releases)
\item \code{is_reexport}: logical indicating whether the symbol is imported via
\code{importFrom()} (i.e., a re-export)
}

Returns \code{NULL} if the symbol is never exported.
}
\description{
This function identifies the earliest version of a package on CRAN that
\strong{explicitly} exports a given symbol in its \code{NAMESPACE} via \code{export()}.
It intentionally ignores \code{exportPattern()} to avoid false positives.
}
\details{
Re-exports are detected by checking for a corresponding \code{importFrom()}
directive for the same symbol.

Results are cached per package and per version to avoid repeatedly
downloading and parsing CRAN tarballs.
}
