% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_first_call_cran.R
\name{get_first_call_cran}
\alias{get_first_call_cran}
\title{Find the first CRAN release containing a call to a function in an R file}
\usage{
get_first_call_cran(
  package,
  fname,
  date_only = FALSE,
  repos = "https://cloud.r-project.org",
  cache_dir = getOption("ggext.cran_cache", file.path(tempdir(), "cran_export_cache"))
)
}
\arguments{
\item{package}{Character. The name of the CRAN package.}

\item{fname}{Character. Function name to search for (e.g. "ggplot").}

\item{date_only}{Logical. If TRUE, return only the CRAN publication Date.}

\item{repos}{Character. CRAN mirror URL.}

\item{cache_dir}{Character. Cache root directory (shared with
\code{get_first_export_cran()}).}
}
\value{
If \code{date_only = TRUE}, a Date or NA.

Otherwise, a one-row data.frame with:
package, fname, version, first_call, file
}
\description{
Scans a package's CRAN source tarballs (current + archive) in chronological
order and returns the earliest release where a call to \code{fname()} appears
in any \verb{R/} source file.
}
\details{
This function shares the same cache root and version index as
\code{get_first_export_cran()}. Once a version has been scanned, its result
is cached permanently and never refreshed.
}
