% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_first_all.R
\name{get_first_all}
\alias{get_first_all}
\title{Get “first seen” dates for a symbol across GitHub + CRAN}
\usage{
get_first_all(user, repo, fname, cran_db = NULL, archive_df = NULL)
}
\arguments{
\item{user}{Character scalar. GitHub owner/user (e.g. \code{"tidyverse"}).}

\item{repo}{Character scalar. GitHub repo name (and assumed CRAN package name).}

\item{fname}{Character scalar. Symbol to look up (e.g. \code{"pivot_longer"}).}

\item{cran_db}{Optional \code{data.frame} returned by \code{\link[tools:CRANtools]{tools::CRAN_package_db()}}.
If \code{NULL}, it is retrieved internally.}

\item{archive_df}{Optional archive lookup \code{data.frame} with columns
\code{package} and \code{archive_date}, typically from \code{\link[=build_archive_df]{build_archive_df()}}.
If \code{NULL}, it is retrieved internally.}
}
\value{
A one-row \code{data.frame} with columns:
\describe{
\item{first_repo}{Earliest commit date in the repository (\code{Date} or \code{NA}).}
\item{first_commit}{Earliest commit date returned by \code{\link[=get_first_commit]{get_first_commit()}} for \code{fname} (\code{Date} or \code{NA}).}
\item{first_gh}{Earliest GitHub export/release date for \code{fname} per \code{\link[=get_first_export_github]{get_first_export_github()}} (\code{Date} or \code{NA}).}
\item{first_release}{Earliest CRAN release date for the package per \code{\link[=get_first_release]{get_first_release()}} (\code{Date} or \code{NA}).}
\item{first_cran}{Earliest CRAN export date for \code{fname} per \code{\link[=get_first_export_cran]{get_first_export_cran()}} (\code{Date} or \code{NA}).}
}
}
\description{
Computes a small one-row summary of earliest known dates across:
\itemize{
\item first commit in the repo (any file),
\item first commit mentioning/introducing \code{fname} (per your \code{get_first_commit()} logic),
\item first GitHub release/export date for \code{fname} (per \code{get_first_export_github()}),
\item first CRAN release date for the package (via \code{\link[=get_first_release]{get_first_release()}}),
\item first CRAN version that explicitly exports \code{fname} (via \code{\link[=get_first_export_cran]{get_first_export_cran()}}).
}
}
\details{
All returned values are \code{Date} scalars (or \code{NA}) and the function returns a
one-row \code{data.frame}.

This function assumes the CRAN package name matches \code{repo}. If that’s not
always true in your workflow, add a separate \code{pkg} argument and pass that to
\code{\link[=get_first_release]{get_first_release()}} and \code{\link[=get_first_export_cran]{get_first_export_cran()}}.
}
\examples{
\dontrun{
get_first_all("tidyverse", "tidyr", "pivot_longer")
}

}
