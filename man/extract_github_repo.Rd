% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_repos.R
\name{extract_github_repo}
\alias{extract_github_repo}
\title{Extract GitHub user and repository from text}
\usage{
extract_github_repo(url_text)
}
\arguments{
\item{url_text}{A single character string containing one or more URLs
or free-form text that may include a GitHub repository link.}
}
\value{
A one-row data frame with two character columns:
\describe{
\item{github_user}{GitHub account name, or \code{NA_character_} if not found}
\item{github_repo}{GitHub repository name, or \code{NA_character_} if not found}
}
}
\description{
Parses a character string (typically derived from CRAN \code{URL} and/or
\code{BugReports} fields) and extracts the first GitHub
\code{user/repository} pair it finds.
}
\details{
The function looks for URLs of the form:
\verb{github.com/<user>/<repo>} (optionally ending in \code{.git}).

If no GitHub repository can be identified, a one-row data frame
with \code{NA} values is returned. The function always returns a
data frame, making it safe to use with \code{purrr::map()} followed by
\code{purrr::list_rbind()}.
}
\examples{
extract_github_repo("https://github.com/tidyverse/ggplot2")

extract_github_repo("Bug reports at https://github.com/r-lib/vctrs/issues")

extract_github_repo(NA_character_)

}
