# Classify CRAN package as in-grammar or other

Classify CRAN package as in-grammar or other

## Usage

``` r
in_grammar_check_cran(package, cran_db = NULL)
```

## Arguments

- package:

  Package name

- cran_db:

  Optional CRAN package database

## Value

"in grammar" or "other"
